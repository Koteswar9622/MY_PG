# Example environment file for backend
# Copy this to `.env` and replace placeholder values with real credentials.
# DO NOT commit your real `.env` file with secrets.

# Example:
# DATABASE_URL=postgresql://<USER>:<PASSWORD>@<HOST>:<PORT>/<DB_NAME>

DATABASE_URL=postgresql://postgres:<REDACTED_PASSWORD>@aws-1-ap-south-1.pooler.supabase.com:6543/postgres
